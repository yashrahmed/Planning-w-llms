[project]
name = "priests-and-cannibals-planner-demo"
version = "0.1.0"
description = "A Unified Planning demo for the priests and cannibals problem."
requires-python = ">=3.10"
dependencies = [
    "unified-planning>=1.3.0",
]
