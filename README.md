# Planning with LLMs (TypeScript + Bun + Python)

## Goal
- Develop techniques to enable LLMs to generate high resolution plans and learn domains and actions in token space.
- This will be done using a combination of classical planner (used by LLMs) and fine tuning.